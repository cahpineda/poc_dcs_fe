---
phase: 07-integration-validation
plan: 02
type: auto
depends_on: ["07-01"]
files_modified:
  - docs/FEATURE-PARITY.md
---

<objective>
Feature parity audit — systematic comparison of all 15 cloud_2 seat_plan features against the React POC.

Purpose: Documents what the POC covers, what it defers, and why. This is the primary artifact for stakeholder sign-off on scope.
Output: `docs/FEATURE-PARITY.md` with per-feature status table and deviation rationale.
</objective>

<context>
@.planning/phases/01-migration-context/MIGRATION-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Feature parity audit document</name>
  <files>docs/FEATURE-PARITY.md</files>
  <action>
    Create `docs/` directory and write `docs/FEATURE-PARITY.md`.

    Map all 15 cloud_2 sub-features from MIGRATION-CONTEXT.md section 2 to their POC status:

    | Status | Meaning |
    |--------|---------|
    | ✅ Implemented | Feature fully delivered in POC |
    | ⚠️ Partial | Core behavior present, edge cases deferred |
    | 🔲 Deferred | Explicitly out of scope for this POC iteration |
    | ❌ Not Applicable | Feature belongs to a different system layer |

    Feature status mapping:
    1. Seat Map Visualization → ✅ (Phase 4: SeatCell, SeatRow, CabinDeck, all 7 status types)
    2. Seat Assignment → ✅ (Phase 5: AssignSeatCommand, useSeatAssign, SeatPlanTab wired)
    3. Seat Reassignment → ⚠️ (Phase 5: ReseatPassengerCommand VO + Cloud2SeatCommandAdapter endpoint; UI reassign flow not wired)
    4. Seat Blocking → ✅ (Phase 5: BlockSeatCommand, useSeatBlock)
    5. Zone-Balanced Seating → ✅ (Phase 5: autoAssignSeat with window/aisle zone scoring)
    6. Seat Pricing → 🔲 (ISeatPricingService stub deferred — requires separate pricing API contract)
    7. Exit Seat Management → ✅ (Phase 4: exit_row_available/occupied statuses, visual differentiation)
    8. Multi-Deck Support → ✅ (Phase 4: DeckToggle, SeatMap activeDeck state)
    9. Seat Occupancy → ✅ (Phase 4: all 7 cloud_2 status codes mapped via STATUS_MAP)
    10. Seat Plan Caching → ✅ (Phase 4: useSeatPlan staleTime=5min, TanStack Query deduplication)
    11. Print Seat Plan → 🔲 (Print/export is a separate controller concern; out of POC scope)
    12. Multi-Leg Availability → 🔲 (Requires flight leg data model; out of POC scope)
    13. IATCI Seating → 🔲 (Partner carrier integration; out of POC scope)
    14. Seat Plan Columns (LC) → ❌ (Load control weight & balance; belongs to separate LC system)
    15. Allocated Seating Flag → ⚠️ (SeatPlanResult.isUpperDeck exists; allocated_seating flag not in current domain model)

    Include sections:
    - Summary table (feature × status)
    - Implementation notes per feature
    - Deferred work backlog (items needing follow-up sprints)
    - Architecture decisions made during POC

    Use `mkdir -p docs` before writing (the Write tool will fail if directory doesn't exist).
    **Use the Write tool, not Bash heredoc.**
  </action>
  <verify>test -f docs/FEATURE-PARITY.md && wc -l docs/FEATURE-PARITY.md</verify>
  <done>File created with all 15 features documented.</done>
</task>

</tasks>

<verification>
- [ ] `docs/FEATURE-PARITY.md` exists with all 15 features
- [ ] Each feature has a status (✅/⚠️/🔲/❌)
- [ ] Deferred items section present
</verification>

<success_criteria>
- Document created and complete
</success_criteria>

<output>
Create `.planning/phases/07-integration-validation/07-02-SUMMARY.md` after completion.
</output>
