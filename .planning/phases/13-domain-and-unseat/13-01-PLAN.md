---
phase: 13-domain-and-unseat
plan: 01
type: tdd
depends_on: ["11-01"]
files_modified:
  - src/domain/seat/Seat.ts
  - src/domain/seat/SeatStatus.ts
  - src/infrastructure/adapters/mappers/seatPlanMapper.ts
  - src/domain/seat/commands/UnassignSeatCommand.ts
  - src/application/ports/ISeatCommandService.ts
  - src/infrastructure/adapters/Cloud2SeatCommandAdapter.ts
  - src/presentation/hooks/useSeatUnassign.ts
  - src/presentation/components/PassengerDetailDrawer.tsx
  - src/presentation/components/SeatPlanTab.tsx
  - src/infrastructure/adapters/MockSeatCommandAdapter.ts
must_haves:
  truths:
    - "Seat domain entity has no passengerId/passenger_key field — resolvePassengerId() synthesizes PAX-<number> as a workaround"
    - "seatPlanMapper maps DTO fields to Seat.create() — adding passenger_key requires updating both"
    - "ISeatCommandService has reseatPassenger — needs unassignSeat added"
    - "Cloud2SeatCommandAdapter calls /ajax/seat_plan/* — unassign calls POST /dc/unseat_passenger"
    - "PassengerDetailDrawer currently has no UNSEAT/unassign button"
    - "boardingGroup, rushStatus, pnr exist in mock data but are not in Seat domain yet"
  artifacts:
    - "Seat domain: passengerKey field added (string | null)"
    - "Seat domain: boardingGroup (number | null), rushStatus (boolean), pnr (string | null) fields added"
    - "seatPlanMapper: maps passenger_key, boarding_group, rush_status, pnr from DTO"
    - "UnassignSeatCommand value object: flightId + seatNumber"
    - "ISeatCommandService: unassignSeat(cmd: UnassignSeatCommand) added"
    - "Cloud2SeatCommandAdapter: unassignSeat calls POST /dc/unseat_passenger"
    - "useSeatUnassign hook: mirrors useSeatAssign, invalidates cache"
    - "PassengerDetailDrawer: UNSEAT button visible for occupied/checked_in/boarded seats"
    - "SeatPlanTab: handleUnassign wired, calls useSeatUnassign, closes drawer"
    - "resolvePassengerId() replaced with seat.passengerKey where available"
  key_links:
    - "src/domain/seat/Seat.ts"
    - "src/infrastructure/adapters/mappers/seatPlanMapper.ts"
    - "src/application/ports/ISeatCommandService.ts"
    - "src/presentation/components/PassengerDetailDrawer.tsx"
    - "src/presentation/components/SeatPlanTab.tsx"
complexity:
  level: medium
  score: 6
  poc_required: false
---

<objective>
Two concerns in one phase:

1. **Real passenger_key** — Add `passengerKey` (and `boardingGroup`, `rushStatus`, `pnr`) to the Seat domain entity and map them from the DTO. This replaces the `resolvePassengerId()` workaround and gives reseat/unassign commands the real cloud_2 key.

2. **Unassign (UNSEAT) operation** — Add `UnassignSeatCommand`, wire it through the port/adapter stack, add `useSeatUnassign` hook, and expose an UNSEAT button in `PassengerDetailDrawer`. This closes Category 4 gap (missing cloud_2 unseat operation).

Both concerns touch the same files (Seat.ts, seatPlanMapper, PassengerDetailDrawer, SeatPlanTab) so batching them avoids a second churn cycle.
</objective>

<execution_context>
@.claude/ink-workflows/workflows/execute-plan.md
@.claude/ink-workflows/templates/summary.md
</execution_context>

<context>
@src/domain/seat/Seat.ts
@src/infrastructure/adapters/mappers/seatPlanMapper.ts
@src/application/ports/ISeatCommandService.ts
@src/infrastructure/adapters/Cloud2SeatCommandAdapter.ts
@src/presentation/components/PassengerDetailDrawer.tsx
@src/presentation/components/SeatPlanTab.tsx
</context>

<tasks>

<feature name="Seat domain — passenger fields">

<task type="tdd">
  <name>Task 1: Add passengerKey + extended fields to Seat entity and mapper</name>
  <complexity>medium</complexity>
  <files>src/domain/seat/Seat.ts, src/infrastructure/adapters/mappers/seatPlanMapper.ts</files>
  <red>
Add tests:
- `Seat.create({ ..., passengerKey: 'PAX-001' }).passengerKey` equals `'PAX-001'`
- `Seat.create({ ..., boardingGroup: 1 }).boardingGroup` equals `1`
- `Seat.create({ ..., rushStatus: true }).rushStatus` equals `true`
- `Seat.create({ ..., pnr: 'ABC123' }).pnr` equals `'ABC123'`
- `seatPlanMapper` test: DTO with `passenger_key: 'PAX-001'` maps to seat with `passengerKey: 'PAX-001'`
All fail — fields don't exist yet.
  </red>
  <green>
In `Seat.ts`:
- Add to `SeatProps`: `passengerKey: string | null`, `boardingGroup: number | null`, `rushStatus: boolean`, `pnr: string | null`
- Expose as readonly getters

In `seatPlanMapper.ts`:
- Map `seat.passenger_key → passengerKey`, `seat.boarding_group → boardingGroup`, `seat.rush_status → rushStatus`, `seat.pnr → pnr`
- Update all `Seat.create()` calls in mapper to pass the new fields (defaulting to null/false for missing DTO fields)
  </green>
  <refactor>
Replace `resolvePassengerId(seat)` in `SeatPlanTab.tsx` with `seat.passengerKey ?? 'PAX-' + seat.number.toString()` and remove the TODO comment.
  </refactor>
  <verify>All mapper tests pass; no TypeScript errors on existing Seat.create() callsites.</verify>
  <done>Seat entity carries real passenger_key + boarding_group + rush_status + pnr</done>
</task>

</feature>

<feature name="UnassignSeat command and hook">

<task type="tdd">
  <name>Task 2: UnassignSeatCommand value object</name>
  <complexity>simple</complexity>
  <files>src/domain/seat/commands/UnassignSeatCommand.ts</files>
  <red>
Write 3 tests:
- `UnassignSeatCommand.create({ flightId: 'FL001', seatNumber: '1A' })` returns instance with correct fields
- Missing `flightId` throws
- Missing `seatNumber` throws
  </red>
  <green>
Create `UnassignSeatCommand.ts` mirroring `BlockSeatCommand` pattern (private constructor, static `create()`, readonly getters for `flightId` and `seatNumber`).
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>3 tests pass.</verify>
  <done>UnassignSeatCommand value object</done>
</task>

<task type="tdd">
  <name>Task 3: Wire unassignSeat through port and adapter</name>
  <complexity>simple</complexity>
  <files>src/application/ports/ISeatCommandService.ts, src/infrastructure/adapters/Cloud2SeatCommandAdapter.ts, src/infrastructure/adapters/MockSeatCommandAdapter.ts</files>
  <red>
Add test: `Cloud2SeatCommandAdapter.unassignSeat()` calls axios.post with `/dc/unseat_passenger` and `{ flight_id, seat_number }`. Test fails — method doesn't exist yet.
  </red>
  <green>
- Add `unassignSeat(cmd: UnassignSeatCommand): Promise<void>` to `ISeatCommandService`
- Implement in `Cloud2SeatCommandAdapter`: `POST /dc/unseat_passenger` with `{ flight_id: cmd.flightId, seat_number: cmd.seatNumber }`
- Implement in `MockSeatCommandAdapter`: resolves after 300ms
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>Adapter test passes; TypeScript confirms interface satisfied.</verify>
  <done>unassignSeat wired from port to adapter</done>
</task>

<task type="tdd">
  <name>Task 4: useSeatUnassign hook</name>
  <complexity>simple</complexity>
  <files>src/presentation/hooks/useSeatUnassign.ts, src/presentation/hooks/__tests__/useSeatUnassign.test.tsx</files>
  <red>
Write 4 tests mirroring `useSeatReseat.test.tsx`:
- hook returns `{ mutate, isPending }`
- `mutate(cmd)` calls `services.seatCommand.unassignSeat(cmd)`
- on success invalidates `['seatPlan', flightId]`
- does NOT invalidate other flight's cache
  </red>
  <green>
Create `useSeatUnassign.ts`: `useMutation` calling `services.seatCommand.unassignSeat`, `onSuccess` invalidates `['seatPlan', flightId]`.
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>4 new tests pass; all existing tests still pass.</verify>
  <done>useSeatUnassign hook</done>
</task>

</feature>

<feature name="PassengerDetailDrawer UNSEAT button">

<task type="tdd">
  <name>Task 5: Add UNSEAT button to drawer and wire in SeatPlanTab</name>
  <complexity>simple</complexity>
  <files>src/presentation/components/PassengerDetailDrawer.tsx, src/presentation/components/SeatPlanTab.tsx, src/presentation/components/__tests__/PassengerDetailDrawer.test.tsx, src/presentation/components/__tests__/SeatPlanTab.test.tsx</files>
  <red>
Add drawer test: when seat status is `occupied`, UNSEAT button is visible.
Add SeatPlanTab test: clicking UNSEAT in drawer calls `unassignMutate` with `UnassignSeatCommand` and closes the drawer.
Both tests fail — button doesn't exist yet.
  </red>
  <green>
In `PassengerDetailDrawer.tsx`:
- Add `onUnassign: (seat: Seat) => void` and `unassignPending: boolean` props
- Show UNSEAT button for `occupied | exit_row_occupied | checked_in | boarded` statuses (same set as block/reseat)
- Button disabled when `unassignPending`

In `SeatPlanTab.tsx`:
- Add `useSeatUnassign` hook, `handleUnassign(seat)` function (calls mutate + closes drawer)
- Pass `onUnassign={handleUnassign}` and `unassignPending={unassignMutation.isPending}` to `PassengerDetailDrawer`
  </green>
  <refactor>Ensure drawer button order: Block / Unblock / Reseat / Unassign — consistent with cloud_2 action order.</refactor>
  <verify>All new tests pass; all 171+ existing tests pass.</verify>
  <done>UNSEAT button visible in drawer, wired end-to-end</done>
</task>

</feature>

</tasks>

<verification>
1. `npm test` — all tests pass (expect ~10 new tests)
2. With mock server running: load FL001, click occupied seat 1B (CARLOS RUIZ), verify UNSEAT button appears
3. Click UNSEAT → seat map reloads, 1B shows as available
4. Verify reseat now uses real `passenger_key` (check network request: `passenger_id` is `PAX-001`, not `PAX-1B`)
</verification>

<success_criteria>
- Seat entity carries passengerKey, boardingGroup, rushStatus, pnr from DTO
- resolvePassengerId() workaround removed
- UnassignSeatCommand value object with validation
- unassignSeat wired through ISeatCommandService → adapters → useSeatUnassign
- UNSEAT button in PassengerDetailDrawer, disabled during pending
- All tests pass
</success_criteria>

<output>Create .planning/phases/13-domain-and-unseat/13-01-SUMMARY.md after completion.</output>
