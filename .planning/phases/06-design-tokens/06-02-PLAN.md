---
phase: 06-design-tokens
plan: 02
type: auto
depends_on: ["06-01"]
files_modified:
  - src/styles/layout.css
  - src/styles/components.css
  - src/styles/feedback.css
---

<objective>
Layout, component, and feedback stylesheets — completing the visual layer for all UI class names used in Phases 4–5.

Purpose: After tokens and seat-states (06-01), the remaining class names (cabin layout, deck toggle, legend, mutation feedback) need styles. Three focused files keep concerns separated.
Output: Full visual coverage for every class name emitted by the seat map components.
</objective>

<context>
@src/presentation/components/CabinDeck.tsx
@src/presentation/components/SeatRow.tsx
@src/presentation/components/DeckToggle.tsx
@src/presentation/components/SeatLegend.tsx
@src/presentation/components/SeatPlanTab.tsx
@src/styles/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cabin layout stylesheet</name>
  <files>src/styles/layout.css</files>
  <action>
    Create `src/styles/layout.css`:

    ```css
    /* Root tab container */
    .seat_plan_tab {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: var(--space-4);
      padding: var(--space-6);
      background: var(--color-surface-alt);
      min-height: 100vh;
      font-family: var(--font-family-base);
    }

    /* Cabin grid */
    .seat_map_container {
      display: flex;
      flex-direction: column;
      gap: var(--seat-gap);
      background: var(--color-surface);
      border: 1px solid var(--color-border);
      border-radius: 8px;
      padding: var(--space-4);
    }

    /* Row */
    .cabin_row {
      display: flex;
      align-items: center;
      gap: var(--seat-gap);
    }

    /* Exit row highlight */
    .exit_row .cabin_row,
    .cabin_row.exit_row {
      background: rgba(255, 152, 0, 0.08);
      border-radius: var(--seat-radius);
      padding: 2px var(--seat-gap);
    }

    /* Row number label */
    .row_number {
      width: 1.5rem;
      font-size: var(--font-size-xs);
      color: var(--color-seat-text-dark);
      text-align: right;
      opacity: 0.5;
      flex-shrink: 0;
    }

    /* Loading / error states */
    .seat_plan_loading,
    .seat_plan_error {
      padding: var(--space-8);
      font-size: var(--font-size-base);
      font-family: var(--font-family-base);
    }

    .seat_plan_error {
      color: var(--color-error);
    }
    ```
  </action>
  <verify>cat src/styles/layout.css | grep "\.seat_map_container"</verify>
  <done>File created with cabin layout rules.</done>
</task>

<task type="auto">
  <name>Task 2: Component stylesheet (DeckToggle + SeatLegend)</name>
  <files>src/styles/components.css</files>
  <action>
    Create `src/styles/components.css`:

    ```css
    /* Deck toggle */
    .deck_toggle {
      display: flex;
      gap: var(--space-2);
      margin-bottom: var(--space-2);
    }

    .deck_toggle button {
      padding: var(--space-2) var(--space-4);
      border: 1px solid var(--color-border);
      border-radius: 4px;
      background: var(--color-surface);
      font-size: var(--font-size-sm);
      font-family: var(--font-family-base);
      cursor: pointer;
      transition: background var(--transition-fast), border-color var(--transition-fast);
    }

    .deck_toggle button:hover {
      background: var(--color-surface-alt);
      border-color: var(--color-brand-secondary);
    }

    .deck_toggle button.deck_active {
      background: var(--color-brand-primary);
      color: var(--color-seat-text-light);
      border-color: var(--color-brand-primary);
    }

    /* Legend */
    .seat_legend {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-4);
      padding: var(--space-3) var(--space-4);
      background: var(--color-surface);
      border: 1px solid var(--color-border);
      border-radius: 6px;
      font-size: var(--font-size-sm);
      font-family: var(--font-family-base);
    }

    .legend_item {
      display: flex;
      align-items: center;
      gap: var(--space-2);
    }

    .legend_swatch {
      width: 1rem;
      height: 1rem;
      border-radius: var(--seat-radius);
      flex-shrink: 0;
    }

    /* Auto-assign button */
    .auto_assign_btn {
      padding: var(--space-2) var(--space-6);
      background: var(--color-brand-secondary);
      color: var(--color-seat-text-light);
      border: none;
      border-radius: 4px;
      font-size: var(--font-size-base);
      font-family: var(--font-family-base);
      font-weight: var(--font-weight-bold);
      cursor: pointer;
      transition: background var(--transition-fast);
    }

    .auto_assign_btn:hover:not(:disabled) {
      background: var(--color-brand-primary);
    }

    .auto_assign_btn:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }
    ```
  </action>
  <verify>cat src/styles/components.css | grep "\.deck_active"</verify>
  <done>File created with DeckToggle and SeatLegend rules.</done>
</task>

<task type="auto">
  <name>Task 3: Feedback stylesheet (mutation status + selection info)</name>
  <files>src/styles/feedback.css</files>
  <action>
    Create `src/styles/feedback.css`:

    ```css
    .seat_assign_pending,
    .seat_assign_success,
    .seat_assign_error,
    .selected_seat_info {
      font-size: var(--font-size-sm);
      font-family: var(--font-family-base);
      padding: var(--space-2) var(--space-4);
      border-radius: 4px;
    }

    .seat_assign_pending {
      background: rgba(245, 124, 0, 0.1);
      color: var(--color-pending);
      border: 1px solid rgba(245, 124, 0, 0.3);
    }

    .seat_assign_success {
      background: rgba(56, 142, 60, 0.1);
      color: var(--color-success);
      border: 1px solid rgba(56, 142, 60, 0.3);
    }

    .seat_assign_error {
      background: rgba(198, 40, 40, 0.1);
      color: var(--color-error);
      border: 1px solid rgba(198, 40, 40, 0.3);
    }

    .selected_seat_info {
      background: rgba(21, 101, 192, 0.1);
      color: var(--color-seat-selected);
      border: 1px solid rgba(21, 101, 192, 0.3);
      font-weight: var(--font-weight-bold);
    }
    ```
  </action>
  <verify>cat src/styles/feedback.css | grep "\.seat_assign_success"</verify>
  <done>File created with all mutation feedback states.</done>
</task>

</tasks>

<verification>
- [ ] `layout.css` covers `.seat_plan_tab`, `.seat_map_container`, `.cabin_row`, `.exit_row`, `.row_number`
- [ ] `components.css` covers `.deck_toggle`, `.deck_active`, `.seat_legend`, `.auto_assign_btn`
- [ ] `feedback.css` covers all 4 feedback states
</verification>

<success_criteria>
- All three files created and complete
</success_criteria>

<output>
Create `.planning/phases/06-design-tokens/06-02-SUMMARY.md` after completion.
</output>
