---
phase: 02-react-scaffold
plan: 03
type: auto
depends_on: ["02-02"]
files_modified:
  - src/domain/seat/index.ts
  - src/application/ports/ISeatPlanQueryService.ts
  - src/application/ports/ISeatCommandService.ts
  - src/application/ports/ISeatPricingService.ts
  - src/application/ports/index.ts
  - src/infrastructure/index.ts
  - src/presentation/components/index.ts
  - src/presentation/hooks/index.ts
  - src/shared/index.ts
complexity:
  level: simple
  score: 3
  poc_required: false
---

<objective>
Create the Screaming Architecture domain folder structure with empty barrel exports and port interface stubs.

Purpose: Establish the structural contract — every future file knows exactly where it lives before any logic is written.
Output: Folder tree matching ARCHITECTURE.md; TypeScript compiles; port interfaces declared (no implementation).
</objective>

<context>
@.planning/PROJECT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-migration-context/MIGRATION-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain folder structure with barrel exports</name>
  <complexity>simple</complexity>
  <files>
    src/domain/seat/index.ts,
    src/application/use-cases/index.ts,
    src/application/ports/index.ts,
    src/infrastructure/index.ts,
    src/presentation/components/index.ts,
    src/presentation/hooks/index.ts,
    src/shared/types/index.ts,
    src/shared/utils/index.ts
  </files>
  <action>
    Create the following directory tree under `src/` with empty `index.ts` barrel files:

    ```
    src/
    ├── domain/
    │   └── seat/
    │       └── index.ts          (export {})
    ├── application/
    │   ├── ports/
    │   │   └── index.ts          (export {})
    │   └── use-cases/
    │       └── index.ts          (export {})
    ├── infrastructure/
    │   └── index.ts              (export {})
    ├── presentation/
    │   ├── components/
    │   │   └── index.ts          (export {})
    │   └── hooks/
    │       └── index.ts          (export {})
    └── shared/
        ├── types/
        │   └── index.ts          (export {})
        └── utils/
            └── index.ts          (export {})
    ```

    Each `index.ts` contains only: `export {};`
    This avoids TS isolated modules error on empty files.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>TypeScript compiles without errors. All directories created.</done>
</task>

<task type="auto">
  <name>Task 2: Declare port interfaces (no implementation)</name>
  <complexity>simple</complexity>
  <files>
    src/application/ports/ISeatPlanQueryService.ts,
    src/application/ports/ISeatCommandService.ts,
    src/application/ports/ISeatPricingService.ts,
    src/application/ports/index.ts
  </files>
  <action>
    Create `src/application/ports/ISeatPlanQueryService.ts`:
    ```ts
    export interface ISeatPlanQueryService {
      getSeatPlan(flightId: string): Promise<unknown>;
      getSeatOccupancy(flightId: string): Promise<unknown>;
    }
    ```

    Create `src/application/ports/ISeatCommandService.ts`:
    ```ts
    export interface ISeatCommandService {
      assignSeat(command: unknown): Promise<void>;
      reseatPassenger(command: unknown): Promise<void>;
      blockSeat(command: unknown): Promise<void>;
      unblockSeat(command: unknown): Promise<void>;
    }
    ```

    Create `src/application/ports/ISeatPricingService.ts`:
    ```ts
    export interface ISeatPricingService {
      getSeatPricing(flightId: string): Promise<unknown>;
    }
    ```

    Update `src/application/ports/index.ts`:
    ```ts
    export type { ISeatPlanQueryService } from './ISeatPlanQueryService';
    export type { ISeatCommandService } from './ISeatCommandService';
    export type { ISeatPricingService } from './ISeatPricingService';
    ```

    Note: `unknown` is intentional — concrete domain types come in Phase 3 (TDD-driven).
    Do NOT define domain types here. Ports reference them as unknown until Phase 3 replaces them.
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -20</verify>
  <done>TypeScript compiles. Three port interface files exist with correct exports.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` passes with no errors
- [ ] `src/domain/seat/`, `src/application/ports/`, `src/infrastructure/`, `src/presentation/`, `src/shared/` all exist
- [ ] Three port interfaces declared: ISeatPlanQueryService, ISeatCommandService, ISeatPricingService
- [ ] `src/application/ports/index.ts` exports all three interfaces
</verification>

<success_criteria>
- All tasks completed
- All verification passes
- No errors introduced
</success_criteria>

<output>
Create `.planning/phases/02-react-scaffold/02-03-SUMMARY.md` after completion.
</output>
