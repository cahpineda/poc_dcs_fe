---
phase: 02-react-scaffold
plan: 01
type: auto
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - src/test/setup.ts
complexity:
  level: simple
  score: 3
  poc_required: false
---

<objective>
Initialize Vite + React 18 + TypeScript 5 project and configure Vitest with jsdom environment.

Purpose: Establish the buildable baseline — running dev server and passing empty test suite.
Output: `npm run dev` serves at localhost:5173; `npm test` passes with 0 tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Vite React-TS project</name>
  <complexity>simple</complexity>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.node.json, index.html</files>
  <action>
    Run: `npm create vite@latest . -- --template react-ts`
    Accept any overwrite prompts. Do NOT modify the generated files yet.
    Verify package.json has `react`, `react-dom`, `typescript`, `vite` as dependencies.
  </action>
  <verify>cat package.json | grep -E '"react"|"vite"|"typescript"'</verify>
  <done>package.json, vite.config.ts, tsconfig.json all exist with Vite template content.</done>
</task>

<task type="auto">
  <name>Task 2: Install Vitest and Testing Library</name>
  <complexity>simple</complexity>
  <files>package.json, vite.config.ts, tsconfig.json, src/test/setup.ts</files>
  <action>
    Run: `npm install`
    Then: `npm install -D vitest @vitest/ui jsdom @testing-library/react @testing-library/user-event @testing-library/jest-dom`

    Configure Vitest in vite.config.ts — add to defineConfig:
    ```ts
    test: {
      globals: true,
      environment: 'jsdom',
      setupFiles: ['./src/test/setup.ts'],
    }
    ```
    Add `/// <reference types="vitest" />` at the top of vite.config.ts.

    Create src/test/setup.ts:
    ```ts
    import '@testing-library/jest-dom';
    ```

    In tsconfig.json compilerOptions, add: `"types": ["vitest/globals"]`
    Add test script to package.json: `"test": "vitest run"`, `"test:ui": "vitest --ui"`
  </action>
  <verify>npm test 2>&1 | tail -5</verify>
  <done>`npm test` exits 0 with "No test files found" or similar passing output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Vite project initialized with Vitest configured</what-built>
  <how-to-verify>
    1. Run: `npm run dev` — verify it opens localhost:5173 without errors
    2. Run: `npm test` — verify it exits 0 (0 tests, no failures)
    3. Press Ctrl+C to stop dev server
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run dev` starts without errors at localhost:5173
- [ ] `npm test` passes with 0 failures
- [ ] `src/test/setup.ts` exists with jest-dom import
- [ ] `vite.config.ts` has `test.environment: 'jsdom'`
</verification>

<success_criteria>
- All tasks completed
- All verification passes
- No TypeScript errors in project
</success_criteria>

<output>
Create `.planning/phases/02-react-scaffold/02-01-SUMMARY.md` after completion.
</output>
