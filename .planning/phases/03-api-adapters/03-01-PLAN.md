---
phase: 03-api-adapters
plan: 01
type: tdd
depends_on: []
files_modified:
  - src/domain/seat/SeatNumber.ts
  - src/domain/seat/__tests__/SeatNumber.test.ts
  - src/domain/seat/index.ts
complexity:
  level: simple
  score: 3
  poc_required: false
---

<objective>
Create the SeatNumber value object with format validation using TDD (RED → GREEN → REFACTOR).

Purpose: Value objects are the safest TDD entry point — pure functions with no dependencies, immediate feedback.
Output: SeatNumber.ts with validated seat identifier; 3 passing tests; committed in 2-3 atomic commits.
</objective>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-migration-context/MIGRATION-CONTEXT.md
</context>

<tasks>
<feature>
  <name>SeatNumber value object</name>
  <files>src/domain/seat/SeatNumber.ts, src/domain/seat/__tests__/SeatNumber.test.ts</files>
  <behavior>
    Happy path: SeatNumber.create("12A") → returns SeatNumber instance with value "12A"
    Null/invalid: SeatNumber.create("") → throws Error("Invalid seat number: empty")
    Error/boundary: SeatNumber.create("ABC") → throws Error("Invalid seat number: ABC") — letters-only has no row number
    Additional: SeatNumber.create("12A").equals(SeatNumber.create("12A")) → true
    Additional: SeatNumber.create("1A").toString() → "1A" — single-digit row valid
  </behavior>
  <implementation>
    Create `src/domain/seat/SeatNumber.ts` as a class with:
    - Private constructor(private readonly value: string)
    - Static create(value: string): SeatNumber — validates format /^\d{1,3}[A-K]$/ before constructing
    - toString(): string — returns value
    - equals(other: SeatNumber): boolean — compares values
    - get rawValue(): string — readonly accessor

    Validation rule: seat number must be 1-3 digits followed by a single uppercase letter A-K.
    This covers all standard aircraft seat identifiers (max 999 rows, columns A-K).

    Export from `src/domain/seat/index.ts`.
  </implementation>
</feature>
</tasks>

<verification>
- [ ] `npm test` passes with all SeatNumber tests green
- [ ] `npx tsc --noEmit` passes — no TypeScript errors
- [ ] Test file has minimum 3 test cases (happy, null/invalid, boundary)
</verification>

<success_criteria>
- Failing test written and committed (RED)
- Implementation passes test (GREEN)
- Refactor complete if needed (REFACTOR)
- 2-3 commits present: test → feat → refactor (optional)
</success_criteria>

<output>
Create `.planning/phases/03-api-adapters/03-01-SUMMARY.md` with:
- RED: What test was written, why it failed
- GREEN: What implementation made it pass
- REFACTOR: What cleanup was done (if any)
- Commits: List of commits produced
</output>
