---
phase: 05-seat-operations
plan: 04
type: auto
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - src/presentation/components/SeatPlanTab.tsx
  - src/presentation/components/SeatMap.tsx
  - src/infrastructure/adapters/MockSeatPlanAdapter.ts
---

<objective>
Wire useSeatAssign and autoAssignSeat into SeatPlanTab — completing the assign + auto-assign flow visible in the dev preview.

Purpose: Connect the command-side hooks built in 05-02 to the existing seat selection UI. Selected seat → assign mutation → cache invalidation → automatic UI refresh via useSeatPlan.
Output: SeatPlanTab fires assignSeat on seat click; auto-assign button calls autoAssignSeat + mutate; loading/error feedback shown inline.
</objective>

<context>
@src/presentation/components/SeatPlanTab.tsx
@src/presentation/hooks/useSeatAssign.ts
@src/domain/seat/autoAssignSeat.ts
@src/infrastructure/adapters/MockSeatPlanAdapter.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useSeatAssign into SeatPlanTab</name>
  <files>src/presentation/components/SeatPlanTab.tsx</files>
  <action>
    Update `SeatPlanTab.tsx`:

    1. Import `useSeatAssign` from `@/presentation/hooks/useSeatAssign`
    2. Import `AssignSeatCommand` from `@/domain/seat/commands/AssignSeatCommand`
    3. Add mutation: `const assignMutation = useSeatAssign(flightId)`
    4. Replace bare `setSelectedSeat` in `onSeatSelect` with a handler that:
       - Sets `selectedSeat` state (for visual highlight)
       - Calls `assignMutation.mutate(AssignSeatCommand.create({ passengerId: 'DEMO-PAX', seatNumber, flightId }))`
       - Note: `passengerId` is hardcoded to 'DEMO-PAX' for POC — real auth wired in Phase 7
    5. Show mutation status below map:
       - `assignMutation.isPending` → `<div className="seat_assign_pending">Assigning…</div>`
       - `assignMutation.isError` → `<div className="seat_assign_error">{assignMutation.error?.message}</div>`
       - `assignMutation.isSuccess` → `<div className="seat_assign_success">Seat assigned</div>`
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -5</verify>
  <done>TypeScript compiles clean. SeatPlanTab calls assignMutation.mutate on seat click.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-assign button</name>
  <files>src/presentation/components/SeatPlanTab.tsx</files>
  <action>
    Import `autoAssignSeat` from `@/domain/seat/autoAssignSeat`.

    Add "Auto-assign" button to the SeatPlanTab JSX (below SeatLegend):
    ```tsx
    <button
      className="auto_assign_btn"
      onClick={() => {
        const recommended = autoAssignSeat(seatPlan);
        if (recommended) {
          setSelectedSeat(recommended);
          assignMutation.mutate(AssignSeatCommand.create({ passengerId: 'DEMO-PAX', seatNumber: recommended, flightId }));
        }
      }}
      disabled={assignMutation.isPending}
    >
      Auto-assign
    </button>
    ```
  </action>
  <verify>npx tsc --noEmit 2>&1 | head -5</verify>
  <done>Button renders, calls autoAssignSeat + mutation on click.</done>
</task>

<task type="auto">
  <name>Task 3: MockSeatPlanAdapter — resolvable assignSeat for dev</name>
  <files>src/infrastructure/adapters/MockSeatPlanAdapter.ts</files>
  <action>
    MockSeatPlanAdapter.assignSeat currently isn't called directly (getSeatPlan is re-fetched after mutation).
    Update `MockSeatPlanAdapter` to implement the full `ISeatCommandService` interface:
    - `assignSeat`, `blockSeat`, `unblockSeat`, `reseatPassenger` all resolve with `undefined` after 300ms delay (simulates network).
    - Wire `MockSeatCommandAdapter` inline in `DependencyProvider.tsx` for `seatCommand` when `import.meta.env.DEV`.

    This lets the dev preview fire the mutation → see "Assigning…" → "Seat assigned" without a real API.
  </action>
  <verify>npm run build 2>&1 | tail -5</verify>
  <done>`npm run build` passes. Dev preview shows mutation feedback on seat click.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full assign flow: click seat → mutation fires → status shown; Auto-assign button selects balanced seat</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Open: http://localhost:5173 (or 5174)
    3. Click an available seat — verify "Assigning…" appears then "Seat assigned"
    4. Click "Auto-assign" — verify a seat is highlighted
    5. Press Ctrl+C
  </how-to-verify>
  <resume-signal>Type "approved" or describe what you see</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run build` passes
- [ ] `npm test` passes (all previous tests still green)
- [ ] SeatPlanTab shows pending/error/success states
- [ ] Auto-assign button visible and functional
- [ ] MockSeatPlanAdapter simulates delay for dev preview
</verification>

<success_criteria>
- All tasks completed and human verify approved
</success_criteria>

<output>
Create `.planning/phases/05-seat-operations/05-04-SUMMARY.md` after human verify.
</output>
