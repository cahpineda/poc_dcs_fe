---
phase: 14-passenger-overlays
plan: 01
type: tdd
depends_on: ["13-01"]
files_modified:
  - src/presentation/components/SeatCell.tsx
  - src/presentation/components/PassengerDetailDrawer.tsx
  - src/presentation/components/CabinDeck.tsx
  - src/styles/components.css
  - src/presentation/components/__tests__/SeatCell.test.tsx
  - src/presentation/components/__tests__/PassengerDetailDrawer.test.tsx
  - src/presentation/components/__tests__/CabinDeck.test.tsx
must_haves:
  truths:
    - "Seat domain now has rushStatus (boolean) and boardingGroup (number | null) after Phase 13"
    - "cloud_2 shows WCHR badge (wheelchair icon or 'W' text) on seats with SSR WCHR"
    - "cloud_2 shows rush-status indicator — a seat is dimmed/marked when rushStatus=true"
    - "PassengerDetailDrawer should show boardingGroup and pnr when available"
    - "CabinDeck currently renders all rows with no visual divider between cabin classes"
    - "cloud_2 shows a cabin class divider row between F/J/Y sections"
    - "Seat.ssrs is already mapped (array of strings) from Phase 9 overlays"
  artifacts:
    - "SeatCell: renders 'W' badge (class seat_ssr_wchr) when seat.ssrs includes 'WCHR'"
    - "SeatCell: applies class seat_rush when seat.rushStatus is true"
    - "PassengerDetailDrawer: shows Boarding Group row when boardingGroup not null"
    - "PassengerDetailDrawer: shows PNR row when pnr not null"
    - "CabinDeck: inserts <CabinDivider> between rows of different cabin classes"
    - "CabinDivider: renders <div class='cabin_divider'> with label (First Class / Business / Economy)"
  key_links:
    - "src/presentation/components/SeatCell.tsx"
    - "src/presentation/components/PassengerDetailDrawer.tsx"
    - "src/presentation/components/CabinDeck.tsx"
complexity:
  level: medium
  score: 5
  poc_required: false
---

<objective>
Layer the remaining passenger-context overlays that exist in cloud_2 but are missing from the POC:

1. **SSR/WCHR badge** — seats with WCHR SSR show a wheelchair indicator, matching cloud_2's `seatHasWchr` CSS class
2. **Rush status** — seats with `rushStatus=true` get a visual marker matching cloud_2's rush indicator 
3. **Boarding group + PNR in drawer** — complete the passenger detail panel with these two fields
4. **Cabin class dividers** — `CabinDeck` inserts a labeled divider between cabin sections (F→J→Y), matching cloud_2's visual cabin separators

These four items address Category 2 (Passenger Overlays) and Category 5 (Seat Information Detail) parity gaps.
</objective>

<execution_context>
@.claude/ink-workflows/workflows/execute-plan.md
@.claude/ink-workflows/templates/summary.md
</execution_context>

<context>
@src/presentation/components/SeatCell.tsx
@src/presentation/components/PassengerDetailDrawer.tsx
@src/presentation/components/CabinDeck.tsx
@src/styles/components.css
</context>

<tasks>

<feature name="SeatCell overlays — SSR and rush">

<task type="tdd">
  <name>Task 1: WCHR SSR badge on SeatCell</name>
  <complexity>simple</complexity>
  <files>src/presentation/components/SeatCell.tsx, src/presentation/components/__tests__/SeatCell.test.tsx</files>
  <red>
Add test: `SeatCell` with `ssrs={['WCHR']}` renders element with class `seat_ssr_wchr`. Test fails — class does not exist.
  </red>
  <green>
In `SeatCell.tsx`: when `seat.ssrs?.includes('WCHR')`, render `<span className="seat_ssr_wchr">W</span>` as an absolute-positioned overlay (bottom-right, similar to existing infant indicator).

Add `.seat_ssr_wchr` to `components.css`: small badge, distinct color (e.g. `--color-info: #0077cc`).
  </green>
  <refactor>Ensure WCHR badge does not overlap with infant icon or gender badge. Position at bottom-right corner.</refactor>
  <verify>Test passes; existing overlay tests unaffected.</verify>
  <done>WCHR badge renders on seats with SSR=WCHR</done>
</task>

<task type="tdd">
  <name>Task 2: Rush status indicator on SeatCell</name>
  <complexity>simple</complexity>
  <files>src/presentation/components/SeatCell.tsx, src/presentation/components/__tests__/SeatCell.test.tsx</files>
  <red>
Add test: `SeatCell` with `rushStatus={true}` has class `seat_rush` on the root cell element. Test fails.
  </red>
  <green>
In `SeatCell.tsx`: add `seat_rush` to root className when `seat.rushStatus === true`.

Add `.seat_rush` to `seat-states.css` or `components.css`: `outline: 2px solid #ff6600; outline-offset: -2px;` (orange outline, cloud_2 uses orange for rush indicator).
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>Test passes.</verify>
  <done>Rush seats have orange outline</done>
</task>

</feature>

<feature name="PassengerDetailDrawer — boarding group + PNR">

<task type="tdd">
  <name>Task 3: Show boardingGroup and PNR in drawer</name>
  <complexity>simple</complexity>
  <files>src/presentation/components/PassengerDetailDrawer.tsx, src/presentation/components/__tests__/PassengerDetailDrawer.test.tsx</files>
  <red>
Add tests:
- Drawer with seat having `boardingGroup: 1` renders text `"Boarding Group"` and `"1"`
- Drawer with seat having `pnr: 'ABC123'` renders `"PNR"` and `"ABC123"`
- Drawer with `boardingGroup: null` does NOT render "Boarding Group" row
Both tests fail — fields not shown.
  </red>
  <green>
In `PassengerDetailDrawer.tsx`, add two conditional `<dt>/<dd>` pairs in the passenger metadata section:
```tsx
{seat.boardingGroup !== null && (
  <><dt>Boarding Group</dt><dd>{seat.boardingGroup}</dd></>
)}
{seat.pnr && (
  <><dt>PNR</dt><dd>{seat.pnr}</dd></>
)}
```
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>3 new tests pass; all existing drawer tests pass.</verify>
  <done>Boarding group and PNR shown in drawer when available</done>
</task>

</feature>

<feature name="Cabin class dividers in CabinDeck">

<task type="tdd">
  <name>Task 4: Insert CabinDivider between cabin class sections</name>
  <complexity>medium</complexity>
  <files>src/presentation/components/CabinDeck.tsx, src/presentation/components/__tests__/CabinDeck.test.tsx, src/styles/components.css</files>
  <red>
Add test: `CabinDeck` rendered with rows spanning cabins F and J renders an element with class `cabin_divider` between the F row and the J row. Test fails — divider not inserted.
  </red>
  <green>
In `CabinDeck.tsx`:
- Track `previousCabinClass` while mapping rows
- When `row.cabinClass !== previousCabinClass` (and it's not the first row), insert `<CabinDivider label={cabinLabel(row.cabinClass)} />`
- `cabinLabel`: `{ F: 'First Class', J: 'Business Class', Y: 'Economy' }`

Create inline `CabinDivider` component (same file):
```tsx
function CabinDivider({ label }: { label: string }) {
  return <div className="cabin_divider"><span>{label}</span></div>;
}
```

Add `.cabin_divider` to `components.css`: full-width row, subtle background, centered label text, `font-size: 0.7rem`, `letter-spacing: 0.05em`.
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>New test passes; all existing CabinDeck tests pass.</verify>
  <done>Cabin dividers inserted at F→J and J→Y boundaries</done>
</task>

</feature>

</tasks>

<verification>
1. `npm test` — all tests pass (expect ~8 new tests)
2. Visual: load FL001 from mock server, verify:
   - Seat 5A (MARIA GARCIA, WCHR SSR) shows W badge
   - Any rush-status seat shows orange outline
   - Cabin dividers visible between F/J/Y sections
   - Click CARLOS RUIZ (1B) → drawer shows boarding group and PNR if set in mock data
</verification>

<success_criteria>
- WCHR badge on SSR seats
- Rush status outline on rush seats
- Boarding group + PNR visible in drawer when available
- Cabin class dividers between F/J and J/Y rows
- All existing tests pass
</success_criteria>

<output>Create .planning/phases/14-passenger-overlays/14-01-SUMMARY.md after completion.</output>
