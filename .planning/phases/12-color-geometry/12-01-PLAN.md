---
phase: 12-color-geometry
plan: 01
type: tdd
depends_on: ["11-01"]
files_modified:
  - src/styles/tokens.css
  - src/styles/seat-states.css
  - src/styles/components.css
  - src/presentation/components/SeatCell.tsx
  - src/presentation/components/__tests__/SeatCell.test.tsx
must_haves:
  truths:
    - "cloud_2 seat colors (from constants_seat_plan.php): available=#4A4D4F, occupied=#947a9c, checked_in=#C1AA02, boarded=#77AE00, infant=darkgreen(#006400)"
    - "cloud_2 seat geometry: side seats 38×35px, middle seats 28×35px (class 'seat_middle'), gender badge 12×12px, block indicator 15×15px"
    - "Current POC uses flat 30×30px cells with approximate colors — does not match cloud_2 exactly"
    - "SeatCell receives 'cabin_class' from Seat domain to determine if side (A/F) or middle (B/C/D/E) seat"
    - "CSS custom properties in tokens.css drive all seat status colors; seat-states.css maps status→token"
  artifacts:
    - "tokens.css updated: --seat-available=#4A4D4F, --seat-occupied=#947a9c, --seat-checked-in=#C1AA02, --seat-boarded=#77AE00, --seat-infant=#006400"
    - "seat-states.css updated: each .seat_<status> uses the new token values"
    - "SeatCell passes isSideSeat computed from seat_number (ends in A or F)"
    - ".seat_cell sizing: 38×35px for side seats (A/F), 28×35px for non-side"
    - ".gender_badge sizing: 12×12px"
    - ".block_indicator sizing: 15×15px"
    - "All 171 existing tests still pass"
  key_links:
    - "src/styles/tokens.css"
    - "src/styles/seat-states.css"
    - "src/presentation/components/SeatCell.tsx"
complexity:
  level: low
  score: 4
  poc_required: false
---

<objective>
Align the React POC color palette and seat cell geometry exactly to cloud_2's values. This closes the biggest visual gap identified in the parity audit (Category 3: Design Token Alignment 5/15 → 15/15) and moves seat geometry from flat squares to proportional rectangles matching the DCS agent's visual memory.

Cloud_2 reference: `constants_seat_plan.php` defines exact hex codes; `dc_seat_plan.css` defines seat widths/heights.

No new features. Pure visual alignment — update CSS tokens and SeatCell sizing logic only.
</objective>

<execution_context>
@.claude/ink-workflows/workflows/execute-plan.md
@.claude/ink-workflows/templates/summary.md
</execution_context>

<context>
@src/styles/tokens.css
@src/styles/seat-states.css
@src/styles/components.css
@src/presentation/components/SeatCell.tsx
</context>

<tasks>

<feature name="Color token alignment">

<task type="tdd">
  <name>Task 1: Update seat status color tokens to cloud_2 exact values</name>
  <complexity>simple</complexity>
  <files>src/styles/tokens.css, src/styles/seat-states.css</files>
  <red>
Write a visual regression snapshot test (or update a SeatCell render test) that asserts the CSS class for `.seat_available` resolves to a background containing `#4A4D4F`. Use `getComputedStyle` or assert the CSS variable value via jsdom. Test must fail with current approximate colors.
  </red>
  <green>
Update `tokens.css`:
```css
--seat-available: #4A4D4F;
--seat-occupied: #947a9c;
--seat-exit-available: #4A4D4F;   /* same base, border distinguishes */
--seat-exit-occupied: #947a9c;
--seat-blocked: #888888;
--seat-unavailable: #cccccc;
--seat-checked-in: #C1AA02;
--seat-boarded: #77AE00;
--seat-infant: #006400;
```

Update `seat-states.css`: ensure each `.seat_<status>` class references the updated token.
  </green>
  <refactor>Remove any hardcoded hex values from seat-states.css that bypass tokens.</refactor>
  <verify>Test passes; no other tests break.</verify>
  <done>9 seat status tokens match cloud_2 exact values</done>
</task>

</feature>

<feature name="Seat cell geometry">

<task type="tdd">
  <name>Task 2: Add isSideSeat logic and proportional cell sizing</name>
  <complexity>simple</complexity>
  <files>src/presentation/components/SeatCell.tsx, src/presentation/components/__tests__/SeatCell.test.tsx</files>
  <red>
Add test: `SeatCell` with `seatNumber="1A"` renders with class `seat_cell_side`; `SeatCell` with `seatNumber="1C"` does NOT have class `seat_cell_side`. Both tests fail — class does not exist yet.
  </red>
  <green>
In `SeatCell.tsx`:
- Derive `isSideSeat` from `seatNumber`: ends in `A` or `F` (regex `/[AF]$/`)
- Add conditional class `seat_cell_side` when `isSideSeat`

In `components.css`, update `.seat_cell`:
```css
.seat_cell { width: 28px; height: 35px; }           /* middle seats */
.seat_cell.seat_cell_side { width: 38px; }          /* side seats override */
```

Update `.gender_badge`: `width: 12px; height: 12px`
Update `.block_indicator`: `width: 15px; height: 15px`
  </green>
  <refactor>Ensure SeatRow gap spacing still looks correct with mixed widths.</refactor>
  <verify>New tests pass; all 171 existing tests pass.</verify>
  <done>Side seats 38×35px, middle seats 28×35px, badges sized to spec</done>
</task>

</feature>

</tasks>

<verification>
1. `npm test` — all 171+ tests pass
2. Visual: start server (`npm run server`), load FL001, verify available seats are charcoal (#4A4D4F), occupied are purple (#947a9c), checked-in are gold (#C1AA02), boarded are green (#77AE00)
3. Visual: A/F column seats visibly wider than B/C/D/E middle seats
</verification>

<success_criteria>
- Color tokens match cloud_2 exact hex values (no more approximations)
- Side seats (A/F) render at 38px wide; middle seats at 28px wide; height 35px for all
- All 171+ existing tests pass without modification
- No new TypeScript errors
</success_criteria>

<output>Create .planning/phases/12-color-geometry/12-01-SUMMARY.md after completion.</output>
