---
phase: 04-seat-map-ui
plan: 01
type: tdd
depends_on: []
files_modified:
  - src/presentation/components/SeatCell.tsx
  - src/presentation/components/AisleGap.tsx
  - src/presentation/components/SeatLegend.tsx
  - src/presentation/components/__tests__/SeatCell.test.tsx
  - src/presentation/components/__tests__/SeatLegend.test.tsx
  - src/presentation/components/index.ts
complexity:
  level: simple
  score: 4
  poc_required: false
---

<objective>
Create SeatCell (atomic seat button) and SeatLegend (status color key) using TDD with React Testing Library.

Purpose: SeatCell is the most-rendered unit in the entire UI — getting its status→class mapping correct by TDD prevents visual regressions in all subsequent rendering plans.
Output: SeatCell renders status CSS classes and fires onSelect; SeatLegend renders all 5 legend entries; 6+ tests GREEN.
</objective>

<context>
@.planning/PROJECT.md
@.planning/phases/01-migration-context/MIGRATION-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@src/domain/seat/SeatStatus.ts
</context>

<tasks>
<feature>
  <name>SeatCell component with status-driven CSS</name>
  <files>
    src/presentation/components/SeatCell.tsx,
    src/presentation/components/AisleGap.tsx,
    src/presentation/components/SeatLegend.tsx,
    src/presentation/components/__tests__/SeatCell.test.tsx,
    src/presentation/components/__tests__/SeatLegend.test.tsx
  </files>
  <behavior>
    Happy path: `<SeatCell seatNumber="12A" status="available" onSelect={fn} />` → renders button with class `seat_available` and text "12A"; clicking calls onSelect with "12A"
    Null/invalid: `<SeatCell seatNumber="12A" status="occupied" onSelect={fn} />` → button has class `seat_occupied` and is NOT clickable (onClick still registered but visually disabled via CSS class)
    Error/boundary: `<SeatCell seatNumber="14F" status="exit_row_available" isExitRow onSelect={fn} />` → has both `seat_available` and `seat_exit` classes
    Additional: `<AisleGap />` → renders a div with class `aisle_spacer` and no interactive elements
    Additional: `<SeatLegend />` → renders 5 entries (available, occupied, blocked, exit_row, selected), each with its CSS class visible
  </behavior>
  <implementation>
    Create `src/presentation/components/SeatCell.tsx`:
    ```tsx
    interface SeatCellProps {
      seatNumber: string;
      status: SeatStatus;
      isExitRow?: boolean;
      isSelected?: boolean;
      price?: number;
      onSelect: (seatNumber: string) => void;
    }
    ```
    - Use `className` combining: base `seat_cell`, status class (`seat_available`, `seat_occupied`, `seat_blocked`, `seat_exit_row_available`, `seat_exit_row_occupied`, `seat_unavailable`, `seat_infant_occupied`), and `seat_exit` when `isExitRow`, `seat_selected` when `isSelected`
    - Status class mapping matches cloud_2 CSS class names from MIGRATION-CONTEXT.md section 7.2
    - `onSelect` called only when status is available or exit_row_available (clickable seats)
    - Shows `price` as a small badge span if provided

    Create `src/presentation/components/AisleGap.tsx`:
    ```tsx
    export function AisleGap() {
      return <div className="aisle_spacer" aria-hidden />;
    }
    ```

    Create `src/presentation/components/SeatLegend.tsx`:
    ```tsx
    // Renders 5 legend entries with seat_available, seat_occupied, seat_blocked, seat_exit, seat_selected classes
    ```

    Use `@testing-library/react` `render` + `screen` for tests. No mocking needed — pure components.
    Export all from `src/presentation/components/index.ts`.
  </implementation>
</feature>
</tasks>

<verification>
- [ ] `npm test` passes with all SeatCell + SeatLegend tests GREEN
- [ ] `npx tsc --noEmit` passes
- [ ] SeatCell applies `seat_exit` class when isExitRow=true
- [ ] AisleGap renders with `aisle_spacer` class and no interactive elements
- [ ] SeatLegend renders exactly 5 legend entries
</verification>

<success_criteria>
- Failing tests written and committed (RED)
- Implementation passes all tests (GREEN)
- Refactor if needed (REFACTOR)
- 2-3 commits: test → feat → refactor (optional)
</success_criteria>

<output>
Create `.planning/phases/04-seat-map-ui/04-01-SUMMARY.md` with RED/GREEN/REFACTOR/Commits sections.
</output>
