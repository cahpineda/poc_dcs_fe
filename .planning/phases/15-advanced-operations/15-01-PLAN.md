---
phase: 15-advanced-operations
plan: 01
type: tdd
depends_on: ["14-01"]
files_modified:
  - src/domain/seat/commands/SwapSeatsCommand.ts
  - src/domain/seat/commands/ReseatGroupCommand.ts
  - src/application/ports/ISeatCommandService.ts
  - src/infrastructure/adapters/Cloud2SeatCommandAdapter.ts
  - src/infrastructure/adapters/MockSeatCommandAdapter.ts
  - src/presentation/hooks/useSeatSwap.ts
  - src/presentation/hooks/useSeatGroupReseat.ts
  - src/presentation/components/SeatPlanTab.tsx
  - src/presentation/components/SeatCell.tsx
  - src/styles/components.css
must_haves:
  truths:
    - "mock server has POST /dc/swap_seats and POST /dc/reseat_group from Phase 11"
    - "cloud_2 dc_seat_plan.js has swapSeats() and groupReseat() operations"
    - "Reseat mode already exists (single passenger) — swap mode is a different UX: click first seat, click second seat"
    - "Group reseat: select multiple passengers then trigger move to available block"
    - "SeatPlanTab manages UI mode state — will need swapMode and groupSelectMode in addition to existing reseatMode"
    - "All existing 171+ tests must still pass"
  artifacts:
    - "SwapSeatsCommand value object: flightId, seatA, seatB"
    - "ReseatGroupCommand value object: flightId, passengerIds[], targetRow?"
    - "ISeatCommandService: swapSeats + reseatGroup methods added"
    - "Cloud2SeatCommandAdapter: both methods implemented"
    - "useSeatSwap hook: swap mode state machine (idle → seat-A-selected → confirm)"
    - "useSeatGroupReseat hook: multi-select state, fires command with collected passengerIds"
    - "SeatPlanTab: swap mode banner + two-click flow + cancel"
    - "SeatPlanTab: group select mode banner + MOVE button + cancel"
    - "SeatCell: swap mode clickable for occupied/available; group mode checkable for occupied seats"
  key_links:
    - "src/domain/seat/commands/"
    - "src/application/ports/ISeatCommandService.ts"
    - "src/presentation/components/SeatPlanTab.tsx"
complexity:
  level: high
  score: 8
  poc_required: false
---

<objective>
Implement the two remaining cloud_2 bulk/agent operations:

1. **Seat swap** — two-click flow: agent clicks an occupied seat (enters swap mode), then clicks target seat; `SwapSeatsCommand` fires; both passengers exchange seats. Matching cloud_2's `swapSeats()` in dc_seat_plan.js.

2. **Group reseat** — agent selects multiple occupied seats (group mode), clicks MOVE GROUP; system finds available block and fires `ReseatGroupCommand`. Matching cloud_2's `groupReseat()`.

These are the final Category 4 (Operations Completeness) items. After this phase, parity score reaches ~95/100.
</objective>

<execution_context>
@.claude/ink-workflows/workflows/execute-plan.md
@.claude/ink-workflows/templates/summary.md
</execution_context>

<context>
@src/application/ports/ISeatCommandService.ts
@src/infrastructure/adapters/Cloud2SeatCommandAdapter.ts
@src/presentation/components/SeatPlanTab.tsx
@src/presentation/components/SeatCell.tsx
</context>

<tasks>

<feature name="SwapSeats command and hook">

<task type="tdd">
  <name>Task 1: SwapSeatsCommand value object</name>
  <complexity>simple</complexity>
  <files>src/domain/seat/commands/SwapSeatsCommand.ts</files>
  <red>
Write 3 tests: valid creation returns correct fields; missing seatA throws; missing seatB throws.
  </red>
  <green>
Create `SwapSeatsCommand.ts` with private constructor, `static create({ flightId, seatA, seatB })`, readonly getters.
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>3 tests pass.</verify>
  <done>SwapSeatsCommand value object</done>
</task>

<task type="tdd">
  <name>Task 2: Wire swapSeats through port and adapter</name>
  <complexity>simple</complexity>
  <files>src/application/ports/ISeatCommandService.ts, src/infrastructure/adapters/Cloud2SeatCommandAdapter.ts, src/infrastructure/adapters/MockSeatCommandAdapter.ts</files>
  <red>
Add test: `Cloud2SeatCommandAdapter.swapSeats()` posts to `/dc/swap_seats` with `{ flight_id, seat_a, seat_b }`. Fails — method missing.
  </red>
  <green>
Add `swapSeats(cmd: SwapSeatsCommand): Promise<void>` to interface and both adapters.
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>Adapter test passes.</verify>
  <done>swapSeats wired through stack</done>
</task>

<task type="tdd">
  <name>Task 3: useSeatSwap hook — two-click state machine</name>
  <complexity>medium</complexity>
  <files>src/presentation/hooks/useSeatSwap.ts, src/presentation/hooks/__tests__/useSeatSwap.test.tsx</files>
  <red>
Write 5 tests:
- `selectFirstSeat('1A')` sets `swapFirstSeat` state
- `selectSecondSeat('2B')` when firstSeat set → calls swapSeats mutation with correct command
- `cancel()` clears state
- on mutation success → state clears
- does not fire if only one seat selected
  </red>
  <green>
Create `useSeatSwap(flightId)`:
- Local state: `firstSeat: string | null`
- Returns: `{ firstSeat, selectSeat(seatNumber), cancel, isPending }`
- `selectSeat`: if `firstSeat` null → set it; else → fire mutation with both seats, clear on success
- `cancel`: clears firstSeat
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>5 tests pass.</verify>
  <done>useSeatSwap state machine hook</done>
</task>

<task type="tdd">
  <name>Task 4: Swap mode UX in SeatPlanTab</name>
  <complexity>medium</complexity>
  <files>src/presentation/components/SeatPlanTab.tsx, src/presentation/components/__tests__/SeatPlanTab.test.tsx</files>
  <red>
Add SeatPlanTab tests:
- SWAP button in drawer (for occupied seat) enters swap mode (banner with "Swap mode: select seat to swap with…" visible)
- Clicking second occupied seat fires swapSeats command with correct seatA/seatB
- Cancel clears mode
  </red>
  <green>
In `SeatPlanTab.tsx`:
- Add `useSeatSwap` hook, expose via `handleSwap(seat)` that sets swap mode with first seat
- Add "SWAP" button to `PassengerDetailDrawer` (new prop `onSwap`)
- In `handleSeatSelect`: when swapMode, second click fires `seatSwap.selectSeat(seatNumber)`
- Add swap mode banner: "Swap mode: select seat to swap with {name}"
- Pass swap mode to SeatMap so non-occupied seats show dimmed

Extend `DRAWER_STATUSES` guard: swap mode clicks on occupied seats only; available seats ignored in swap mode.
  </green>
  <refactor>Extract mode priority logic into a helper: `getActiveMode(reseatMode, swapMode, groupMode)` to keep handleSeatSelect readable.</refactor>
  <verify>New tests pass; all existing tests pass.</verify>
  <done>Swap mode fully wired in SeatPlanTab</done>
</task>

</feature>

<feature name="ReseatGroup command and hook">

<task type="tdd">
  <name>Task 5: ReseatGroupCommand value object</name>
  <complexity>simple</complexity>
  <files>src/domain/seat/commands/ReseatGroupCommand.ts</files>
  <red>
Write 3 tests: valid creation; missing passengerIds throws; empty passengerIds throws.
  </red>
  <green>
Create `ReseatGroupCommand.ts` with `{ flightId, passengerIds: string[], targetRow?: number }`.
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>3 tests pass.</verify>
  <done>ReseatGroupCommand value object</done>
</task>

<task type="tdd">
  <name>Task 6: Wire reseatGroup through port and adapter</name>
  <complexity>simple</complexity>
  <files>src/application/ports/ISeatCommandService.ts, src/infrastructure/adapters/Cloud2SeatCommandAdapter.ts, src/infrastructure/adapters/MockSeatCommandAdapter.ts</files>
  <red>
Add test: `Cloud2SeatCommandAdapter.reseatGroup()` posts to `/dc/reseat_group` with `{ flight_id, passenger_ids, target_row }`. Fails — method missing.
  </red>
  <green>
Add `reseatGroup(cmd: ReseatGroupCommand): Promise<void>` to interface and both adapters.
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>Adapter test passes.</verify>
  <done>reseatGroup wired through stack</done>
</task>

<task type="tdd">
  <name>Task 7: useSeatGroupReseat hook — multi-select state</name>
  <complexity>medium</complexity>
  <files>src/presentation/hooks/useSeatGroupReseat.ts, src/presentation/hooks/__tests__/useSeatGroupReseat.test.tsx</files>
  <red>
Write 5 tests:
- `toggleSeat('PAX-001')` adds to selected set
- `toggleSeat('PAX-001')` again removes it (toggle)
- `confirm()` with 2 selected passengers fires `reseatGroup` with correct command
- `cancel()` clears selection
- Requires at least 1 passenger to fire (empty selection does not call mutate)
  </red>
  <green>
Create `useSeatGroupReseat(flightId)`:
- State: `selectedPassengerIds: Set<string>`
- Returns: `{ selectedPassengerIds, togglePassenger(id), confirm(), cancel(), isPending }`
  </green>
  <refactor>No refactor needed.</refactor>
  <verify>5 tests pass.</verify>
  <done>useSeatGroupReseat multi-select hook</done>
</task>

<task type="tdd">
  <name>Task 8: Group reseat mode UX in SeatPlanTab</name>
  <complexity>medium</complexity>
  <files>src/presentation/components/SeatPlanTab.tsx, src/presentation/components/__tests__/SeatPlanTab.test.tsx</files>
  <red>
Add tests:
- "GROUP RESEAT" toolbar button puts app in group select mode (banner visible: "Group select: N passengers selected")
- Clicking occupied seat in group mode toggles selection (checkbox-style visual)
- "MOVE GROUP" button fires reseatGroup command; mode clears on success
- Cancel clears mode
  </red>
  <green>
In `SeatPlanTab.tsx`:
- Add `useSeatGroupReseat` hook
- Add "GROUP RESEAT" button in toolbar (next to Auto-assign)
- `groupMode` derived: `groupReseat.selectedPassengerIds.size >= 0 && groupActive` (boolean flag)
- Banner: "Group select: {N} passengers selected" with MOVE GROUP + Cancel buttons
- In `handleSeatSelect`: when `groupMode` and seat is occupied, `groupReseat.togglePassenger(seat.passengerKey!)`
- Pass `groupMode` and `selectedPassengerIds` down to SeatCell for visual checkbox indicator
  </green>
  <refactor>If `handleSeatSelect` exceeds 30 lines after all modes, extract each mode branch to named functions.</refactor>
  <verify>New tests pass; all existing tests pass.</verify>
  <done>Group reseat mode fully wired</done>
</task>

</feature>

</tasks>

<verification>
1. `npm test` — all tests pass (expect ~24 new tests, ~200 total)
2. With mock server running (FL001):
   - Click occupied seat → drawer shows SWAP button
   - Click SWAP → banner appears, click another occupied seat → both passengers exchanged (GET shows updated state)
   - Click GROUP RESEAT → group mode banner, click 2 occupied seats → MOVE GROUP fires → seats reassigned
3. Cancel flows work for both modes
</verification>

<success_criteria>
- SwapSeatsCommand + ReseatGroupCommand value objects with validation
- Both commands wired through ISeatCommandService → Cloud2 + Mock adapters
- useSeatSwap two-click state machine (5 tests)
- useSeatGroupReseat multi-select (5 tests)
- Full UX for both modes in SeatPlanTab with banners and cancel
- All 171+ existing tests still pass
- Grand total: ~200 tests
</success_criteria>

<output>Create .planning/phases/15-advanced-operations/15-01-SUMMARY.md after completion.</output>
